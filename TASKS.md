# Tasks

- [ ] Get an icon for this project (ask Alina)
- [ ] Generate `metadata.json` file at `S3_BUCKET/[project]/metadata.json` containing the entire metadata store as JSON. Clients should prefer accessing this file instead of using the `/api` endpoint. Need to keep it up to date.
- [ ] **Maybe:** Deploy on AWS instead of Vercel
- [ ] Change main URL from `https://crow-cms.vercel.app` to `https://crowcms.vercel.app`. Remember to update alikro.art and CloudFront distributions as well.
